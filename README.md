# Distributed information systems - homework 2

Start docker images with databases: `docker compose up -d`
Create virtual python environment: `python -m venv venv`

All code and queires are in dbs_test.ipynb.